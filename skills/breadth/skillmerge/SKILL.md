---
name: skillmerge
description: "비슷한 스킬들을 통폐합한다: 입력·출력·읽고 쓰는 표·판단기준을 대조해 같은 일을 하는 스킬을 찾고, 합침·흡수·체인·유지 계획을 보고한 뒤 승인 후에만 합친다. '중복 스킬 정리', '비슷한 스킬이 여러 개', '팀원들이 각자 만든 걸 하나로', 여러 팩에 걸쳐 스킬이 흩어져 있을 때 사용. 감사는 읽기 전용, 변경은 명시적 승인 뒤에만."
---

같은 일을 하는 스킬을 찾아 하나로 줄인다. 이어붙이지 않고 깨끗한 v0로 다시 쓴다.

## Goal

팀원이 각자 만든 스킬은 이름만 다르고 같은 일을 하는 경우가 많다. 그대로 묶으면 같은 표에 둘이 쓰고, 같은 규칙이 다른 값으로 두 곳에 산다. `skillmerge`는 스킬 수를 늘리지 않고 줄인다. 하나의 일에는 하나의 스킬(SSOT). 합칠 때는 두 본문을 이어붙이지 않고 팀 관점의 v0로 재구조화하며, 뺀 스킬은 지우지 않고 보관한다.

`ssotize`처럼 읽기 전용 감사로 시작해 계획을 보고하고, 승인 뒤에만 파일을 바꾼다.

## Workflow

1. 범위를 고정한다. 팩 하나, 또는 여러 폴더.
2. **유사도 감사**(읽기 전용). `python3 check/similarity.py <경로...>`를 돌리거나, 없으면 손으로 세 축을 대조한다: (a) 입력·출력 이름 겹침, (b) 읽고 쓰는 표 겹침, (c) 판단기준·본문 키워드 겹침. 표기 변형(띄어쓰기·언더스코어·하이픈)은 같은 것으로 본다.
3. 쌍마다 분류한다:
   - **동일** — 입력·출력·표가 같다 → 합침 후보.
   - **포함** — 한쪽이 다른 쪽의 부분이다 → 흡수 후보.
   - **인접** — 한쪽의 출력이 다른 쪽의 입력이다 → 합치지 않는다. `weave`로 넘겨 체인으로 잇는다.
   - **동명이인** — 입출력은 같은데 판단기준이 다르다 → 합치지 않는다. 갈림길이므로 `weave`에서 경로로 다룬다.
   - **무관** — 그대로 둔다.
4. 합침·흡수 후보마다 계획을 쓴다: 남길 이름(계약의 정본), 판단기준·예외의 합집합, 담당자(둘이면 팀 결정), 폐기할 스킬의 보관 위치, 갱신할 참조(`next`, `CONTRACT.md`의 writers·chain·payloads).
5. **보고**: 표(쌍 · 근거 · 분류 · 계획)와 팀 결정 필요 목록. 명시적 승인을 요청한다. 승인이 없으면 여기서 멈춘다.
6. 승인 후 합친다. 새 `SKILL.md`를 v0로 쓴다(두 본문의 합집합을 재구조화; 예외 목록은 합집합, 판단기준은 하나로). 폐기 스킬은 `archive/<이름>/`로 옮기고 이유 한 줄을 남긴다. `next`와 계약을 갱신한다.
7. `mergechk`를 돌려 계약 게이트가 🟢인지 확인한다.
8. `planning/`이 있으면 합침 결정과 근거를 `findings.md`의 Technical Decisions에 적는다(`planfiles`).

## Rules

- 감사가 먼저, 변경은 승인 뒤. 계획 보고 전에 파일을 고치지 않는다.
- 찾은 게 없으면 아무것도 바꾸지 않는다. 빈 결과는 유효하다. 통폐합을 정당화하려고 유사도를 부풀리지 않는다.
- 판단기준이 다르면 합치지 않는다. 입출력이 같아도 규칙이 다르면 다른 스킬이다.
- 잃는 정보가 없어야 한다. 한쪽에만 있던 예외·판단기준은 합친 스킬에 들어간다.
- 삭제가 아니라 보관. 폐기한 스킬은 `archive/`로 옮긴다. 실패한 갈래도 다음 판의 자료다.
- 담당자 충돌, 임계값 상충은 팀 결정이다. 조용히 한쪽을 고르지 않는다.
- 편집 안전: 바꿀 대상이 있는지 확인하고 없으면 MISS로 보고한다. 유니코드 안전하게(`PYTHONUTF8=1`), 한 건씩 바꾼다.
- 한 번 합친 뒤에는 `re0`로 유지한다. 통폐합은 반복 정비가 아니라 한 번의 수리다.

## Verification

끝내기 전에:

1. 계획이 보고되고 승인된 뒤에만 변경했거나, 읽기 전용으로 끝났다.
2. 합친 스킬이 두 원본의 판단기준·예외를 모두 담고, 이어붙인 흔적 없이 v0로 읽힌다.
3. 폐기 스킬이 `archive/`에 있고 이유가 적혀 있다.
4. `next`·`CONTRACT.md`·기획서의 참조가 새 이름을 가리키고, 계약 게이트가 🟢이다.
5. 무엇을 합쳤고, 무엇을 체인으로 넘겼고, 무엇이 팀 결정으로 남았는지 보고했다.
